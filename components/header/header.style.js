import styled from 'styled-components'

const HeaderWrapper = styled.div`
  display: flex;
  flex-wrap: wrap;
  div {
    align-self: center;
  }
  img {
      width: 170px;
      margin: -47px 0 0 0;
      position: absolute;
  }
`;

export default HeaderWrapper